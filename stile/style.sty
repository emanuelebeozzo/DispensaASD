% Codifica
\usepackage[utf8]{inputenc}

\usepackage{cancel}
\usepackage{amssymb}
\usepackage{makecell}
\usepackage[a4paper, total={7in, 10in}]{geometry}
\usepackage{xcolor}
\usepackage{textcomp}
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

% Caption
\usepackage{caption}
\DeclareCaptionFormat{mylst}{\hrule#1#2#3}
\captionsetup[lstlisting]{
	format=mylst,
	labelfont=bf,
	singlelinecheck=off,
	labelsep=space
}

% Pseudocodice
\usepackage{listings}
\usepackage{xcolor}
\usepackage{amsmath}
 
% style per lo pseudocodice (listing)
\lstset{
	basicstyle=\small\ttfamily,
	backgroundcolor=\color{white},
	basicstyle=\footnotesize,
	breakatwhitespace=false,
	numbers=left,
	numbersep=5pt,
	xleftmargin=20pt,
	numberstyle=\tiny\color{codegray},
	frame=tb,
	framexleftmargin=20pt,
	keywordstyle=\color{magenta}\bfseries,
	breaklines=true,
	commentstyle=\color{codegreen},
	morekeywords={int, char, float, boolean, char, new, if, else, iif, then, do, $\in$, to, downto, foreach, while, for, return, nil, delete, print, precondition, and, or, not, mod, true, false},
	morecomment=[l]{\%},
	mathescape=true,
	showspaces=false,
	stringstyle=\color{blue},
	morestring=[s]""
}
% Rename listing
\renewcommand\lstlistingname{Algorithm}
\renewcommand\lstlistlistingname{Algorithms}

% Per alberi binari
\usepackage{tikz}
\usetikzlibrary{trees}

% Per i link della tabella dei contenuti
\usepackage[hidelinks]{hyperref}
\hypersetup{
    linktoc=all
}